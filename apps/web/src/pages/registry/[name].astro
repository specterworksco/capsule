---
import BaseLayout from "@/layouts/BaseLayout.astro";

export async function getStaticPaths() {
  // For static build, provide an empty set. In production with SSR,
  // these would be fetched dynamically from the Registry API.
  return [];
}
---

<BaseLayout title="Package - Capsule Registry">
  <div class="max-w-4xl mx-auto px-6 py-16">
    <p class="text-muted-foreground">
      Package details are loaded dynamically. Visit <a href="/registry" class="text-primary">the registry page</a> to search for packages.
    </p>
  </div>
</BaseLayout>
